
## Remover arquivo já rastreado posteriormente

1. Insira o nome correto do arquivo no .gitignore

2. Use o seguinte comando para remover o arquivo do controle de versão do Git:

```
git rm --cached .DS_Store
```

isso remove o arquivo .DS_Store do controle de versão, mas ele ainda permanece 
em seu diretório local.

3. Faça um `commit` das mudanças com uma mensagem descritiva:

```
git commit -m "Remover .Rproj.user do controle de versão"
``

4. Em seguida, faça um push das mudanças para o GitHub:

```
git push
``
